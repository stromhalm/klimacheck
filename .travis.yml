dist: "trusty"
sudo: "required"
language: "node_js"
node_js: "6"
os: "linux"
install:
 - npm install -g firebase-tools
 - npm install -g angular-cli
 - npm install
script:
 - echo "0"
after_success:
 - ng build --prod
 - firebase deploy --token "$FIREBASE_TOKEN"